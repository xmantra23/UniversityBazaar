<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Activity.Messages.MessageHomeActivity">

    <RelativeLayout
        android:id="@+id/relativeLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/border"
        android:paddingLeft="20dp"
        android:paddingTop="20dp"
        android:paddingRight="20dp"
        android:paddingBottom="60dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.496"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.236">

        <TextView
            android:id="@+id/txtHeader"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="25dp"
            android:text="Please pick an option."
            android:textSize="25sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnInbox"
            android:layout_width="250dp"
            android:layout_height="wrap_content"
            android:layout_below="@+id/txtHeader"
            android:layout_marginTop="44dp"
            android:text="Inbox"
            app:backgroundTint="#2697F1"
            app:cornerRadius="18dp" />

        <Button
            android:id="@+id/btnSentMessages"
            android:layout_width="250dp"
            android:layout_height="wrap_content"
            android:layout_below="@+id/btnInbox"
            android:layout_marginTop="40dp"
            android:text="Sent Messages"
            app:backgroundTint="#2697F1"
            app:cornerRadius="18dp" />

        <Button
            android:id="@+id/btnNewMessage"
            android:layout_width="250dp"
            android:layout_height="wrap_content"
            android:layout_below="@+id/btnSentMessages"
            android:layout_marginTop="40dp"
            android:text="New message"
            app:backgroundTint="#2697F1"
            app:cornerRadius="18dp" />

    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/homeRelLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        app:layout_constraintEnd_toEndOf="@+id/relativeLayout"
        app:layout_constraintStart_toStartOf="@+id/relativeLayout"
        app:layout_constraintTop_toBottomOf="@+id/relativeLayout"
        android:background="@drawable/border"
        android:padding="20dp">

        <Button
            android:id="@+id/btnHome"
            android:layout_width="250dp"
            android:layout_height="wrap_content"
            android:text="Home page"
            app:backgroundTint="#4CA550"
            app:cornerRadius="18dp" />

    </RelativeLayout>


</androidx.constraintlayout.widget.ConstraintLayout>